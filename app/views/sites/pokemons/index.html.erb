<p style="color: green"><%= notice %></p>

<% content_for :title, "Pokemons" %>

<h1>Pokemons</h1>

<div id="sites_pokemons">
  <% @sites_pokemons.each do |sites_pokemon| %>
    <%= render sites_pokemon %>
    <p>
      <%= link_to "Show this pokemon", sites_pokemon %>
    </p>
  <% end %>
</div>

<%= link_to "New pokemon", new_sites_pokemon_path %>
